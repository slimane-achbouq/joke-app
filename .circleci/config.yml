version: 2.1

orbs:
  node: circleci/node@5.0.2

workflows:
  build:
    jobs:
      - node/test:
          filters:
            branches:
              only:
                - cicleci
    
      - node/run:
          npm-run: build
          filters:
            branches:
              only:
                - cicleci